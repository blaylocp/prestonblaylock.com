import { imageUrl } from '../../../helpers/image-url';
import { module, test } from 'qunit';

module('Unit | Helper | image url');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = imageUrl([42]);
  assert.ok(result);
});
