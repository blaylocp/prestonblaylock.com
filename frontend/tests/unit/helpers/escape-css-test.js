import { escapeCss } from '../../../helpers/escape-css';
import { module, test } from 'qunit';

module('Unit | Helper | escape css');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = escapeCss([42]);
  assert.ok(result);
});
